# --- Configura��o do Banco de Dados H2 ---

server.address=0.0.0.0
server.port=8080

# Habilita o console web do H2
#spring.h2.console.enabled=true
# Define a URL para acessar o console
#spring.h2.console.path=/h2-console

# URL de conex�o JDBC. 'mem' significa em mem�ria. 'karaokedb' � o nome do nosso banco.
#spring.datasource.url=jdbc:h2:mem:karaokedb
# Driver JDBC do H2
#spring.datasource.driverClassName=org.h2.Driver
# Usu�rio padr�o do H2
#spring.datasource.username=admin
# Senha (pode deixar em branco, mas � bom definir para o console)
#spring.datasource.password=123

# --- Configura��o do JPA (Hibernate) ---

# Diz ao Hibernate qual "dialeto" SQL usar para se comunicar com o H2
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# MUITO IMPORTANTE: Define como as tabelas do banco ser�o criadas.
# 'update': atualiza o schema (tabelas) se houver mudan�as nos seus @Entity.
# 'create-drop': cria as tabelas ao iniciar e as apaga ao finalizar. �timo para testes.
#spring.jpa.hibernate.ddl-auto=create-drop

# Show SQL for debugging tests
#spring.jpa.show-sql=true
#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE


# ===================================================================
# CONFIGURAÇÃO HÍBRIDA (LOCAL vs PRODUÇÃO)
# ===================================================================

# 1. URL DE CONEXÃO
spring.datasource.url=jdbc:postgresql://ep-shy-silence-aiprw7hw-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require

# 2. USUÁRIO
spring.datasource.username=neondb_owner

# 3. SENHA
spring.datasource.password=npg_acL4mGSpEjT2

# ===================================================================
# JPA & HIBERNATE
# ===================================================================
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
# Em produção, mude para false para não poluir os logs do Render
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=false

# --- Configuração do JWT (JSON Web Token) ---

# Chave secreta: Tenta ler da env JWT_SECRET, senão usa uma chave Base64 válida de 256 bits para dev
jwt.secret-key=${JWT_SECRET:YTNmZDE2ZWY4ZjkzNGU4YmE0YmUyZGE5Zjk4YzU0MzI1YjA3Zjc5MjBkMDhiMDU5YzQwMGVlNWU2YjUzYjUyMA==}
# Tempo de expiração do token em milissegundos (7200000 ms = 2 horas)
jwt.expiration=${JWT_EXPIRATION:7200000}

# ---- API KEY DO YOUTUBE ---- # 

# Lógica: Pega da env YOUTUBE_API_KEY. Se não houver, usa a chave de dev (apenas para debug local)
youtube.api.key=${YOUTUBE_API_KEY:AIzaSyAVAycqwz5UEDrE4cYNByMj4froM_LdBhU}


spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true
spring.jackson.charset=UTF-8